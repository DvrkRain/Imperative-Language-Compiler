type IntArray is array[3] integer;

routine inc_first(a : array [] integer)
is
    a[1] := a[1] + 1;
end

routine inc_value(x : integer)
is
    x := x + 1;
end

var arr : IntArray;
arr[1] := 10;

print arr[1];
